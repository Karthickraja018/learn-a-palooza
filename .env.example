# Set a password here to protect the "organizer setup" panel that edits drafts.
ADMIN_SECRET=changeme

# KV_REST_API_URL / KV_REST_API_TOKEN (or UPSTASH_REDIS_REST_URL /
# UPSTASH_REDIS_REST_TOKEN) are injected automatically once you connect a
# Redis database (via the Upstash Marketplace integration) to this project
# in the Vercel dashboard -> Storage tab. You do not need to set them by hand.
